window.D42PE_RSVP_CONFIG = Object.freeze({
  // Intentionally blank until the authorized Worker + D1 deployment exists.
  // Local development uses the current origin automatically.
  apiBaseUrl: ""
});
